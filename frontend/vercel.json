{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://votechain-backend-ueuj.onrender.com/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
